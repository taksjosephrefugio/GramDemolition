set number              " Turn on line numbers
set hlsearch            " Turn on search highlighting
set tabstop=4           " Tab stops at every 4th column
set shiftwidth=4        " Tab shifts by 4 characters
set nowrapscan          " Search does not wrap back to top when bottom has been reached
